let colorLens = document.querySelectorAll(".quadrant");

let start = document.querySelector;
let sequencePattern = [];

for (let i = 0; i < colorLens.length; i++) {
  sequencePattern.push([i]);
}

console.log(sequencePattern);

// reset form event? for restarting game?

console.log(sequencePattern[2]);
